<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:KlubSportowy.Views">

    <BooleanToVisibilityConverter x:Key="BoolToVis" />

    <SolidColorBrush x:Key="PrimaryBrush" Color="#B80006"/>
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#8E0005"/>
    <SolidColorBrush x:Key="BackgroundLightBrush" Color="#F4F7F9"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#DCE1E5"/>

    <Style x:Key="ModernButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="15,6"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,2"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToolbarToggleStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="15,6"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,2"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#333"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:WszystkieViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:WszystkieViewBase}">
                    <Grid Background="{StaticResource BackgroundLightBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15,15,15,5">
                            <ToggleButton Content="➕ Dodaj" IsChecked="{Binding IsAdding, Mode=TwoWay}" Style="{StaticResource ToolbarToggleStyle}"/>
                            <Button Content="📝 Edytuj" Style="{StaticResource ModernButtonStyle}"/>
                            <Button Content="🗑️ Kasuj" Style="{StaticResource ModernButtonStyle}"/>
                            <Button Content="🔄 Odśwież" Command="{Binding LoadCommand}" Style="{StaticResource ModernButtonStyle}"/>
                        </StackPanel>

                        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" Margin="15,5" CornerRadius="8" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
                            <Expander Header="Filtrowanie i Sortowanie" Padding="10">
                                <UniformGrid Columns="3">
                                    <StackPanel Margin="10,0">
                                        <Label Content="Szukaj w:"/>
                                        <ComboBox ItemsSource="{Binding FilterColumnOptions}" SelectedItem="{Binding SelectedFilterColumn}"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,0">
                                        <Label Content="Fraza:"/>
                                        <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
                                    </StackPanel>
                                    <StackPanel Margin="10,0">
                                        <Label Content="Sortuj:"/>
                                        <ComboBox ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SortBy}"/>
                                    </StackPanel>
                                </UniformGrid>
                            </Expander>
                        </Border>

                        <ContentPresenter Grid.Row="2" Margin="15,5,15,15"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:JedenViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:JedenViewBase}">
                    <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="12,12,0,0" BorderThickness="0,4,0,0" BorderBrush="{StaticResource PrimaryBrush}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#FDFDFD" Padding="15,10" CornerRadius="12,12,0,0">
                                <Button Content="💾 Zapisz i zamknij" Command="{Binding SaveAndCloseCommand}" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Left"/>
                            </Border>
                            <ContentPresenter Grid.Row="1" Margin="20"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Height" Value="30"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="true">
                <Setter Property="ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                <Setter Property="BorderBrush" Value="Red"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>