<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:KlubSportowy.Views">

    <SolidColorBrush x:Key="ButtonNormalBrush" Color="#B80006"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#EF0107"/>
    <SolidColorBrush x:Key="ButtonPressedBrush" Color="#800004"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextBrush" Color="#FFFFFF"/>

    <Style x:Key="ToolbarButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="{StaticResource ButtonNormalBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonNormalBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5,2"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonPressedBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="{StaticResource ButtonPressedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:WszystkieViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:WszystkieViewBase}">
                    <Grid Margin="0,10,0,0" Background="{StaticResource SurfaceBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <WrapPanel Grid.Row="0" Height="30" Margin="0,2,0,2" VerticalAlignment="Center">
                            <ToggleButton Content="Edytuj" Height="30" Width="70" Style="{StaticResource ToolbarButtonStyle}"/>
                            <ToggleButton Content="Kasuj" Height="30" Width="70" Style="{StaticResource ToolbarButtonStyle}"/>
                            <ToggleButton Content="Odswiez" Height="30" Width="70" Command="{Binding LoadCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
                        </WrapPanel>
                        <Expander Grid.Row="1" Background="{StaticResource SurfaceBrush}" Header="Sortowanie i filtrowanie">
                            <Label Foreground="{StaticResource ButtonNormalBrush}">Tu bedzie filtrowanie i sortowanie</Label>
                        </Expander>
                        <ContentPresenter Grid.Row="2" Margin="0,5,0,5"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:JedenViewBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:JedenViewBase}">
                    <Grid Margin="0,10,0,0" Background="{StaticResource SurfaceBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <WrapPanel Grid.Row="0" Height="30" Margin="0,2,0,2" VerticalAlignment="Center">
                            <ToggleButton Content="Dodaj" Width="70" Height="30" Command="{Binding SaveAndCloseCommand}" Style="{StaticResource ToolbarButtonStyle}"/>
                            <ToggleButton Content="Anuluj" Width="70" Height="30" Style="{StaticResource ToolbarButtonStyle}"/>
                        </WrapPanel>
                        <ContentPresenter Grid.Row="1" Margin="0,5,0,5"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
