<local:WszystkieViewBase x:Class="KlubSportowy.Views.DruzynyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KlubSportowy.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <local:WszystkieViewBase.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </local:WszystkieViewBase.Resources>

    <Grid Background="white">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}"
                  Grid.Row="0"
                  Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding DruzynaId}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}"/>
                <DataGridTextColumn Header="Kategoria" Binding="{Binding Kategoria}"/>
                <DataGridTextColumn Header="TrenerId" Binding="{Binding TrenerId}"/>
                <DataGridTextColumn Header="CzyAktywny" Binding="{Binding CzyAktywny}"/>
                <DataGridTextColumn Header="KtoDodal" Binding="{Binding KtoDodal}"/>
                <DataGridTextColumn Header="KiedyDodal" Binding="{Binding KiedyDodal}"/>
                <DataGridTextColumn Header="KtoModyfikowal" Binding="{Binding KtoModyfikowal}"/>
                <DataGridTextColumn Header="KiedyModyfikowal" Binding="{Binding KiedyModyfikowal}"/>
                <DataGridTextColumn Header="KtoWykasowal" Binding="{Binding KtoWykasowal}"/>
                <DataGridTextColumn Header="Uwagi" Binding="{Binding Uwagi}"/>
            </DataGrid.Columns>
        </DataGrid>

        <local:JedenViewBase Grid.Row="1" Visibility="{Binding IsAdding, Converter={StaticResource BoolToVis}}">
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Grid.Column="0" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Nazwa"/>
                    <TextBox Grid.Column="1" Text="{Binding Nazwa, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="0" Grid.Column="1" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Kategoria"/>
                    <TextBox Grid.Column="1" Text="{Binding Kategoria, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="0" Grid.Column="2" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="TrenerId"/>
                    <TextBox Grid.Column="1" Text="{Binding TrenerId, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="0" Grid.Column="3" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="CzyAktywny"/>
                    <TextBox Grid.Column="1" Text="{Binding CzyAktywny, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="0" Grid.Column="4" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="KtoDodal"/>
                    <TextBox Grid.Column="1" Text="{Binding KtoDodal, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="0" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="KiedyDodal"/>
                    <TextBox Grid.Column="1" Text="{Binding KiedyDodal, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="1" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="KtoModyfikowal"/>
                    <TextBox Grid.Column="1" Text="{Binding KtoModyfikowal, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="2" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="KiedyModyfikowal"/>
                    <TextBox Grid.Column="1" Text="{Binding KiedyModyfikowal, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="3" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="KtoWykasowal"/>
                    <TextBox Grid.Column="1" Text="{Binding KtoWykasowal, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="4" Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Uwagi"/>
                    <TextBox Grid.Column="1" Text="{Binding Uwagi, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Grid>
        </local:JedenViewBase>
    </Grid>
</local:WszystkieViewBase>