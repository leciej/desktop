<UserControl x:Class="KlubSportowy.Views.WybierzDruzyneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding List}" SelectedItem="{Binding SelectedDruzyna}" 
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding DruzynaId}" Width="50"/>
                <DataGridTextColumn Header="Nazwa Drużyny" Binding="{Binding Nazwa}" Width="*"/>
                <DataGridTextColumn Header="Kategoria" Binding="{Binding Kategoria}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="1" Content="Wybierz" Command="{Binding WybierzCommand}" 
                Width="100" HorizontalAlignment="Right" Margin="10" Height="30"/>
    </Grid>
</UserControl>